# App-local overrides (repo root .gitignore applies to all paths below).
# Keep this file minimal; prefer adding patterns in the root .gitignore.

# Payload upload dir (also listed at repo root)
/media
