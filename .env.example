# Root env for `docker compose` variable substitution. Copy to `.env` if you override defaults.
# Host port for this repo’s Postgres only (change if 54332 is taken on your machine).
CONTORRO_DB_PORT=54332
